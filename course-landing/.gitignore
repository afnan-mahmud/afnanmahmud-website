# This app shares the repo-root node_modules — none should exist here.
/node_modules
/.next
/out
next-env.d.ts

# Keep the env template tracked (the repo-root .gitignore's ".env*" would hide it).
!.env.example

# Design reference screenshots — not used by the app.
demo-design/
